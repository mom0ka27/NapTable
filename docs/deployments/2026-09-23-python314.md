# Python 3.14 / SQLite runtime upgrade

- Switched at 2026-09-23 00:42 Asia/Taipei.
- Release: `/opt/naptable/releases/20260922T163946Z-python314`.
- Previous release retained: `20260922T161525Z-8c7a4fbe-dirty-95201`.
- Runtime: CPython 3.14.7, SQLite 3.53.1, cryptography 46.0.7.
- uv-managed runtime under `/opt/naptable/python-managed`, stable link `/opt/naptable/python`; OS Python and SQLite libraries unchanged.
- Cloned the existing production server code into a new release and created a new virtual environment; no unrelated workspace server changes deployed.
- Updated backup unit to use the current release's interpreter. Local deployment script now requires Python 3.14 and SQLite >= 3.35 to prevent runtime regression.

Validation: all 163 server tests passed under the new Linux runtime. The transport-failure test previously depended on cross-thread listener closure cancelling Linux accept(); it now injects connection refusal deterministically. No application-code change was required.

Stopped service before final database backup (`naptable-20260922T164222Z.sqlite3`), switched release, restarted and verified health. Backup service passed with the new runtime and produced `naptable-20260922T164223Z.sqlite3`. Database quick_check=ok; systemd active, NRestarts=0, ExecMainStatus=0.

Both localhost and public HTTPS health and broadcast-config requests returned HTTP 200. The SQLite syntax exception / HTTP 502 is resolved. Broadcast channels completed provisioning: both current schools return status=ready, with 11 and 12 sandbox channels respectively, through both localhost and public HTTPS. No manual test pushes were sent.
