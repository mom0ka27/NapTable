# 后台界面更新

- Release：`20260923T094414Z-8c7a4fbe-dirty-35587`
- 前一版本：`20260923T091726Z-8c7a4fbe-dirty-23037`
- 地址：https://naptable.mom0ka27.top/admin

重做管理台样式：深绿侧栏、浅色卡片、清晰的字段层级、学校目录与编辑分栏、重新排版的登录页及推送配置指南。登录后自动选中第一所学校。统计页增加真实学校占比环图、系统版本和型号占比条形图，以及明确的无数据状态。静态资源版本为 `20260923-ui2`。

浏览器检查使用临时数据库和本机 Chromium，所有模拟学校和设备数据仅存在本地，没有写入生产：

- 登录、Cookie 会话恢复、学校新增/保存/删除、统计刷新、学校筛选通过。
- 空统计和 24 台模拟设备的图表、占比与数值一致。
- 320、390、768、1440 像素下的学校、统计、调休和推送页均无横向溢出。
- 320 像素的节次输入框和新建学校弹窗未裁切。
- 移动导航支持 Escape 关闭及焦点恢复；隐藏导航不参与键盘交互，退出登录会关闭侧栏并聚焦令牌输入框。
- 无 JavaScript 页面异常；Node 语法检查与 git diff --check 通过。
- 服务端现有 17 项 HTTP/会话/配置测试通过。

部署完成数据库预检与备份。公网 HTML、CSS、JS 均返回 200，SHA-256 与本地逐字节一致；公网健康检查 200。systemd active/running，NRestarts=0，ExecMainStatus=0。本机 curl 的 TLS 问题仍存在，Python HTTPS 和服务端公网检查正常。

截图：`docs/screenshots/admin-login.png`、`admin-schools.png`、`admin-statistics.png`。截图来自临时本地环境，统计页展示无数据状态。
