# 独立使用统计与客户端隐私引导

- 服务端 Release：`20260923T091726Z-8c7a4fbe-dirty-23037`
- 前一版本：`20260923T083619Z-8c7a4fbe-dirty-96231`
- 服务地址：https://naptable.mom0ka27.top
- 当前工作区服务端已部署；客户端已编译验证，未发布 iOS 安装包。

新增独立 usage_devices 表和 `/v1/usage/devices/{installationUUID}` 接口。管理员统计改为近 30 天上报过基础信息的安装数，提供学校、系统版本、设备型号分布及未关联设备数；不从实况通知记录推断用户许可。90 天未上报的数据在统计写入/查询时清理。后台支持按学校筛选设备分布。

客户端新增版本化的基础隐私协议与可选实时通知信息上传许可，两项默认均未勾选。基础许可同意前不挂载主界面，不上传使用统计；首次正常进入需导入含课程的课表。第二项可稍后开启实时通知时授权，也可在隐私设置撤回；撤回关闭本地活动并清理服务端注册和计划，离线时重试。升级用户的历史活动在缺少许可时停止；已有课程无需重新导入。

验证：

- Python 169 项测试完成，1 项跳过；覆盖设备去重、属性更新、分布、凭据、许可版本、最小化字段、30/90 天窗口和持久化。
- Swift 引导/统计检查通过：未授权无请求、基本许可不代表通知许可、空导入不能完成、重复上报节流、失败重试、HTTPS、许可持久化与撤回。
- Swift 实时活动与网络协调器检查通过，包含无许可不注册、撤回后仅执行 DELETE 清除请求。
- iOS 模拟器目标 Xcode 编译通过。独立模拟器截图验证隐私页和导入页，未导入无法进入主界面；基础许可前没有生成统计或通知设备 ID。测试模拟器随后移除。
- 线上 `/health`、`/admin`、`/static/admin.js`、已认证 `/v1/admin/stats` 均为 200；统计返回 windowDays=30 及系统/型号分布字段。
- 未提供基础许可版本的统计请求返回 400，未创建测试记录。
- 线上统计初始为 0，等待用户安装新版客户端并明确同意协议；旧通知设备没有自动迁入统计。
- 数据库 quick_check=ok；systemd active/running，NRestarts=0，ExecMainStatus=0。
- 已完成迁移副本预检及发布前后数据库备份；发布后备份为 `naptable-20260923T091736Z.sqlite3`。
- 本机 curl 出现 TLS 错误；服务器 HTTPS 及本机 Python HTTPS 均通过。

界面截图：`docs/screenshots/onboarding-privacy.png`、`docs/screenshots/onboarding-import.png`。
