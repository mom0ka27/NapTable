# 学校管理发布记录

- Release：`20260923T083619Z-8c7a4fbe-dirty-96231`
- SSH 目标：`nap`
- 前一版本：`20260922T163946Z-python314`
- 地址：https://naptable.mom0ka27.top
- 使用当前工作区的服务端文件部署；未提交或推送 Git，未发布 iOS 客户端。

新增学校直接持久化，提供管理员学校删除接口及按钮；删除学校同时删除其学期，保留已有分享快照。移除后台自动广播频道展示。学校目录初始化改为一次性，升级清理旧版自动内置的中国药科大学，删除学校后不再随重启恢复。

复用本次修改后已完成的 165 项服务端测试（1 项跳过），部署时不重复运行。Python 3.14.7 / SQLite 3.53.1 保持不变。安装脚本完成数据库副本迁移预检、停服务后的数据库备份、版本切换及部署后备份。最新备份：`naptable-20260923T083629Z.sqlite3`。

线上验证：

- systemd active/running，NRestarts=0，ExecMainStatus=0。
- 公网 `/health`、`/v1/schools`、`/admin`、`/static/admin.js` 均返回 200。
- 学校目录仅包含 `nju / 南京大学`。
- 管理页包含删除按钮，不包含“自动广播频道”；JS 包含删除处理逻辑。
- 数据库 quick_check=ok，学校目录迁移标记已写入。
- 本机 curl 出现连接重置；服务器公网检查和本机 Python HTTPS 检查均成功。
